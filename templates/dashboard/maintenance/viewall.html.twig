{% extends 'dashboard/dashboardbase.html.twig' %}

{% block panel %}

<div class="dashboard-subheader">
  <div class="container-fluid container-fixed-lg">
    <div class="row">
      <div class="col-lg-12">
        <div class="breadcrumb-container">
          <span class="breadcrumb">Maintenance</span>
        </div>
        <a class="action-btn btn btn-primary pull-right" href="/dashboard/maintenance/add">Add Maintenance</a>
      </div>
    </div>
  </div>
</div>
<div class="dashboard-panels">
  <div class="container-fluid container-fixed-lg">
    <div class="row">
      <div class="col-lg-12">
        <table id="maintenance-view-table" class="uk-table uk-table-hover uk-table-striped is-hidden">
          <thead>
            <tr>
              <th>Title</th>
              <th>Created</th>
              <th></th>
            </tr>
          </thead>
          <tbody>

            {% for record in maintenance %}
            <tr data-id="{{ record.hashId }}">
              <td data-order="{{ record.name }}">
                <div class="list-item">
                  <div class="list-item-decoration">
                    <i class="list-item-icon status-icon status-icon-{{ record.status.type }}"></i>
                  </div>
                  <div class="list-item-content">
                    <a class="list-item-title" href="/dashboard/maintenance/{{ record.hashId }}">{{ record.name }}</a>
                    <span class="list-item-createdby">{{ record.createdBy.fullName }}</span>
                    <span class="list-item-description ellipsis-3">{{ record.purpose }}</span>
                  </div>
                </div>
              </td>
              <td>{{ record.created|date("m/d/Y g:ia") }}</td>
              <td>
                <button class="btn btn-error btn-delete pull-right">Delete</button>
                <a class="btn btn-primary pull-right" href="/dashboard/maintenance/{{ record.hashId }}">Update</a>
              </td>
            </tr>
            {% endfor %}

          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

{% endblock %}
